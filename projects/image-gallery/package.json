{
  "name": "image-gallery",
  "version": "1.0.0",
  "description": "Playing around with tailwind",
  "main": "index.js",
  "scripts": {
    "start": "tailwindcss -i ./input.css -o ./css/output.css --watch",
    "build": "tailwindcss -i ./input.css -o ./css/output.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bhushan",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
